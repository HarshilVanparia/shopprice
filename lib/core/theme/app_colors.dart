import 'package:flutter/material.dart';

class AppColors {
  // Premium Dark Palette
  static const Color primary = Color(0xFF6B9EFF);        // Bright Blue
  static const Color secondary = Color(0xFFFFB347);      // Warm Gold
  static const Color tertiary = Color(0xFF7C3AED);       // Purple Accent
  static const Color neutral = Color(0xFF6B7280);        // Medium Gray

  // Background - Dark & Premium
  static const Color background = Color(0xFF0F1419);     // Deep Black
  static const Color surface = Color(0xFF1A1F2E);        // Dark Gray
  static const Color surfaceVariant = Color(0xFF252D3D); // Medium Dark
  static const Color onBackground = Color(0xFFFFFFFF);   // White Text
  static const Color onSurface = Color(0xFFE5E7EB);      // Light Gray Text
  static const Color error = Color(0xFFEF4444);          // Bright Red

  // Accents
  static const Color divider = Color(0xFF3A4452);
  static const Color success = Color(0xFF10B981);        // Emerald Green
}

